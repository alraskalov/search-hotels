import { FC } from 'react';
import './Login.css';

export const Login: FC = () => {
  return(
  <div className="login page__login">
    <form className='form' action="">
      <input type="text" />
    </form>
  </div>
  );
};
